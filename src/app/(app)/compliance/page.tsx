import ComingSoon from "@/components/ComingSoon";

export default function CompliancePage() {
  return (
    <ComingSoon
      title="Compliance Calendar"
      subtitle="All filings, all clients, all deadlines — one calendar"
      rung="A unified calendar across GST, TDS, and ITR deadlines, due after those modules land."
    />
  );
}
