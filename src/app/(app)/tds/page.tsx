import ComingSoon from "@/components/ComingSoon";

export default function TdsPage() {
  return (
    <ComingSoon
      title="TDS · Form 26AS Reconciliation"
      subtitle="26AS vs books · auto-match by PAN + section · Form 16A generation"
      rung="Builds on the TDS certificates the parser extracts from the inbox."
    />
  );
}
