/** The seeded demo firm. Used while auth is off (rungs 1–4); rung 5 replaces
 *  this with the signed-in user's firm via currentFirmId(). */
export const DEMO_FIRM_ID = "11111111-1111-1111-1111-111111111111";
